//--------------------------------------------------
// Hola Mundo Processing   
// Enrique Rodriguez Vela  (enero-2018)
//--------------------------------------------------
void setup() { //<>//
  size(500, 500); // Tamaño de la pantalla
  noStroke(); //borde cuando pulsas
  fill(0); // para no rellenar con un color
}

void draw() {
  background(900); //Color del fondo RGB
  
  
  //if (mousePressed) {
  //  if (mouseX < width/2) {
  //   // rect(0, 0, width/2, height); // Left
  //    rect(30, 20, 250, 250);
  //  } else {
  //    rect(width/2, 0, width/2, height); // Right
  //  }
  //}
}